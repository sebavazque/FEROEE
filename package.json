{"proxy": "http://localhost:3001",
  "name": "mp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "wat": "npx nodemon index" ,
    "start": "node index.js",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "16.14.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "mercadopago": "^1.5.14"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
